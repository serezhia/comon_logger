## 0.1.2

- Moved the package into the shared `packages/` workspace layout and refreshed repository metadata and documentation links.
- Revalidated the Flutter renderer package against the unified workspace, lint, and release tooling.

## 0.1.1

- Maintenance release for workspace version alignment.

## 0.1.0

- Initial release of comon_logger_dio_flutter.
- Flutter UI renderer for comon_logger Dio HTTP logs.

