## 0.1.2

- Moved the extension package into the shared `packages/` workspace layout and refreshed repository metadata, DevTools config, and web manifest text.
- Removed template leftovers and revalidated the package against the unified workspace and analyzer setup.

## 0.1.1

- Maintenance release for workspace version alignment.

## 0.1.0

- Initial release of comon_logger_devtools_extension.
- DevTools extension for comon_logger.

