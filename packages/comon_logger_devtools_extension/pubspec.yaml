name: comon_logger_devtools_extension
description: DevTools extension for comon_logger — live log viewer with filters.
version: 0.1.2
publish_to: "none"
resolution: workspace
repository: https://github.com/serezhia/comon_logger/tree/main/packages/comon_logger_devtools_extension

environment:
  sdk: ^3.9.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  comon_logger: ^0.1.2
  devtools_app_shared: ^0.3.0
  devtools_extensions: ^0.3.0
  vm_service: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
